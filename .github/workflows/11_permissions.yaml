name: 11 permission

on:
  workflow_dispatch:

jobs:
  example-job:
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Attempt write Operation
        run: |
          touch new-file.txt
          git config --global user.name 'WisdomNwaiwu'
          git config --global user.email 'wisdom.nwaiwu.peer@gmail.com'
          git add new-file.txt
          git commit -m "Attempt to write"
          git push origin ${{ github.ref }}